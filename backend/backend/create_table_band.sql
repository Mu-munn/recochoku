CREATE TABLE band (
    -- 主キー（プライマリキー）
    id int auto_increment PRIMARY KEY,
    bandName VARCHAR(100),
    bandMemberNum INT,
    musicGenre INT,
    skills INT,
    residence INT
);
    
    
    -- instruments INT,